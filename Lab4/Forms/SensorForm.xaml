<Window x:Class="Lab4.Forms.SensorForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Створення датчика"
        Height="300"
        Width="350"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Тип величини:"
                   Margin="5"/>

        <ComboBox x:Name="QuantityTypeComboBox"
                  Grid.Column="1"
                  Margin="5"/>

        <TextBlock Grid.Row="1"
                   Text="Мінімум:"
                   Margin="5"/>

        <TextBox x:Name="MinValueTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="5"/>

        <TextBlock Grid.Row="2"
                   Text="Максимум:"
                   Margin="5"/>

        <TextBox x:Name="MaxValueTextBox"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="5"/>

        <TextBlock Grid.Row="3"
                   Text="Поточне:"
                   Margin="5"/>

        <TextBox x:Name="CurrentValueTextBox"
                 Grid.Row="3"
                 Grid.Column="1"
                 Margin="5"/>

        <Button Grid.Row="4"
                Grid.ColumnSpan="2"
                Content="Створити"
                Width="120"
                Height="30"
                Margin="5"
                HorizontalAlignment="Center"
                Click="CreateButton_Click"/>

    </Grid>
</Window>