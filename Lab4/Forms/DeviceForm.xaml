<Window x:Class="Lab4.Forms.DeviceForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Створення пристрою"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="5"
                   Text="Місце кріплення:"/>

        <TextBox x:Name="PositionTextBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="5"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="5"
                   Text="Серійний номер:"/>

        <TextBox x:Name="SerialNumberTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="5"/>

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="5"
                   Text="Дата калібрування:"/>

        <DatePicker x:Name="CalibrationDatePicker"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="5"/>

        <!-- Кнопка створення датчика -->
        <Button x:Name="SensorButton"
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Margin="5,15,5,5"
                Height="30"
                Content="Створити датчик"
                Click="SensorButton_Click"/>

        <Button Grid.Row="4"
                Grid.ColumnSpan="2"
                Width="120"
                Height="30"
                Margin="5,20,5,5"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Content="Створити"
                Click="CreateButton_Click"/>

    </Grid>
</Window>